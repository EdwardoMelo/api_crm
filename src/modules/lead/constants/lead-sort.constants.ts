export enum LeadSortField {
  CREATED_AT = 'createdAt',
  NOME = 'nome',
  EMAIL = 'email',
  EMPRESA = 'empresa',
  STATUS = 'status',
}

export const DEFAULT_LEAD_SORT_FIELD = LeadSortField.CREATED_AT;
